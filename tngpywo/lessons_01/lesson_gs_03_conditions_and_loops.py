from tngpywo import raise_exercise_not_yet_started


def your_first_if_clause(x):
    """
    #core - easy

    - Print the string "HIT" if `x` equals 42.
    - Print "OK" if `x` is any other non-negative number.
    - Print "FAIL" it is negative.

    >>> your_first_if_clause(42)
    "HIT"
    """
    raise_exercise_not_yet_started()


def your_second_if(x, start, end):
    """
    #core - easy

    Print True if x lies between start and end (or is equal to either),
    otherwise print False.

    >>> your_second_if(42, 0, 100)
    True
    """
    raise_exercise_not_yet_started()


def your_first_for_loop(message, n):
    """
    #core - easy

    Print the string `message` exactly `n`-times.

    >>> your_first_for_loop("Hyvää päivää", 3)
    Hyvää päivää
    Hyvää päivää
    Hyvää päivää
    """
    raise_exercise_not_yet_started()


def count_to_n(n: int):
    """
    #core - easy

    Print numbers from 1 to `n` using a while loop.

    >>> count_to_n(3)
    1
    2
    3
    """
    raise_exercise_not_yet_started()


def your_second_for_loop(text: str):
    """
    #core - medium

    Print every character in `text`, one character per line.

    >>> your_second_for_loop("Python is great!")
    P
    y
    t
    h
    o
    n
    """
    raise_exercise_not_yet_started()


def sum_until_limit(limit: int):
    """
    #extra credit - medium

    Sum numbers starting from 1 until the sum exceeds `limit`.
    Print the final sum.

    >>> sum_until_limit(10)
    15  # 1+2+3+4+5 = 15
    """
    raise_exercise_not_yet_started()
