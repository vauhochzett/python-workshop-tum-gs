from tngpywo import raise_exercise_not_yet_started


def a_list(one, two, three):
    """
    #core - easy

    Return a list of the three numbers given.

    >>> a_list(1, 2, 3)
    [1, 2, 3]
    """
    raise_exercise_not_yet_started()


def your_first_element(data):
    """
    #core - easy

    Return the first element of sequence `data`.

    >>> your_first_element([1, 2, 3])
    1

    >>> your_first_element([0])
    0
    """
    raise_exercise_not_yet_started()


def get_last_element(data):
    """
    #core - easy

    Return the last element of sequence `data`.

    >>> get_last_element([1, 2, 3])
    3

    >>> get_last_element(["a", "b", "c"])
    "c"
    """
    raise_exercise_not_yet_started()


def return_some_dict(key1, value1, key2, value2):
    """
    #core - easy

    Return the dictionary that maps `key1` to `value1` and `key2` to `value2`.

    >>> r = return_some_dict("a", 1, "b", 2)
    {"a": 1, "b": 2}
    """
    raise_exercise_not_yet_started()


def return_dict_keys(d: dict):
    """
    #core - easy

    Return the keys of dict `d`.
    >>> return_dict_keys({"a": 1, "b": 22, "cc": "x"})
    ["a", "b", "cc"]
    """
    raise_exercise_not_yet_started()


def first_and_last(data):
    """
    #core - easy

    Return a list consisting of the first and the last element of sequence `data`.

    >>> first_and_last([1, 2, 3])
    [1, 3]

    >>> first_and_last(["a"])
    ["a", "a"]
    """
    raise_exercise_not_yet_started()


def remove_from_dict(dictionary):
    """
    #core - easy

    Remove the item with key "a" from `dictionary`.

    >>> d = {"a": 1, "b": 2, "c": "3"}
    >>> remove_from_dict(d)
    >>> d
    {"b": 2, "c": "3"}
    """
    raise_exercise_not_yet_started()


def slicing_it(data, start, end):
    """
    #core - intermediate

    Return a slice of the sequence `data` from `start` to `end`

    >>> slicing_it([1, 2, 3], 1, 2)
    [2, 3]

    >>> slicing_it(["a", "b"], 0, 1)
    ["a", "b"]
    """
    raise_exercise_not_yet_started()


def return_dict_values(d: dict):
    """
    #extra credit - intermediate

    Return the values of dict `d`.

    >>> return_dict_values({"a": 1, "b": 22, "cc": 333})
    [1, 22, 333]
    """
    raise_exercise_not_yet_started()


def without_first_and_last(data):
    """
    #extra credit - intermediate

    Return the sequence `data` without its first and its last element.

    >>> without_first_and_last([1, 2, 3])
    [2]

    >>> without_first_and_last(["a", "b"])
    []
    """
    raise_exercise_not_yet_started()
